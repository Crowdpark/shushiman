package{	import flash.display.MovieClip;	import flash.display.DisplayObject;	import flash.filters.DisplacementMapFilter;	import flash.utils.getQualifiedClassName;	import flash.geom.Point;		/**	 * @author szalwinski	 */	 	public class LevelGenerator extends MovieClip	{		private var _log:String = "";		private var _number:uint = 0;		private var _fields:Array = [];		private var _prevField:Field;		private var _rowNew:Boolean = true;		private var _startX:Number = 20;		private var _startY:Number = 20;		private var _sizeX:Number = 20;		private var _sizeY:Number = 20;				public function LevelGenerator()		{			_log += '{ ';			level();			gather();			convert();			_log += "}}";			trace(_log);		}				private function level():void		{			_log += '"level":{';			_log += '"id":' + _number + ',';		}				private function gather():void		{			for (var i:uint=0; i < _sizeX; i++)			{				for (var j:uint = 0; j < _sizeY; j++)				{					var exists:Boolean = objects.getObjectsUnderPoint(new Point(40*j,40*i)).length > 0;					if (exists)					{						var object:DisplayObject = objects.getObjectsUnderPoint(new Point(40*j, 40*i))[0].parent;						trace(j, i, object);						var field:Field = new Field();						field.id = i;						field.x = object.x;						field.y = object.y;						field.type = getQualifiedClassName(object);						trace(field.type);						_fields.push(field);					}				}							}		}				private function convert():void		{			_log += '"fields":[[';			_rowNew = true;						for (var i:uint = 0; i < _fields.length-1; i++)			{				var field:Field = _fields[i];								if (_prevField)				{					if (_prevField.x > field.x)					{												_log += '],[';						_rowNew = true;					}					else					{						_rowNew = false;					}				}				else				{					_rowNew = true;				}						if (_rowNew)				{					_log += field;				}				else				{					_log += "," + field;				}								_rowNew = false;				_prevField = field;			}						_log += "]]";		}	}}